// Calculate the area of a rectangle?

#include <stdio.h>
int main(){
    float length, breadth;
    printf("Eter length & breadth of rectangle: ");
scanf("%f%f", &length, &breadth)0;

 float area = length * breadth;
printf("Area of rectangle is:%f " area);

return length * breadth;
}