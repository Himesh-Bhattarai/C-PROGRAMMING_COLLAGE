// Find the smallest of two numbers
#include <stdio.h>
int main()
{
    printf("Enter two numbers to compare which is smallest: ");
    scanf("%d%d", num1,num2);

    if(num1 < num2 ){
        printf("%d is smaller as compare to %d", num1, num2);
    }else{
        printf("%d is smaller as compare to %d", num2, num3);

    }

    return 0;
}
