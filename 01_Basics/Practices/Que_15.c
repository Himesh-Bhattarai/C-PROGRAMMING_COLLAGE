// Check whether a number is even or odd?
#include <stdio.h>
int main(){
    int Numbers;
    printf("Enter The number As your wish without decimals mumbers: \n");
    scanf("%d", &Numbers);

    printf(if (Numbers / 2 == 0){
        printf("NUmber is Even\n");
        else{
            printf("Number is Odd.\n");
        }
    });
    return 0;
}