// Calculate the perimeter of a square?
#include <stdio.h>
int main(){
    float perimeter, side;
    printf("Value of side of square: ");
    scanf("%f",&side);

    perimeter = 4 * side;

    printf("Perimeter of Suqare is : %f", perimeter);
}